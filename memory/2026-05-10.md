- Projeto DGES/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.
- Projeto DGAS/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.

## site-universidade — estado em 2026-05-10

- Projeto em `projects/site-universidade/`, Next.js App Router + TypeScript + Tailwind CSS, static export para Cloudflare Pages.
- Data source: Google Sheet `1m7LzrYoYTrCHYr3vBiaeK62ZOw-4GQVCbXse6d5FE6E`, importada via `gog` para `data/courses.csv`.
- Stack: Next.js, TypeScript, Tailwind CSS, Recharts (gráfico de notas), Papa Parse (CSV).
- Rotas geradas: `/`, `/cursos/`, `/cursos/[slug]/`, `/faculdades/`, `/faculdades/[slug]/`, `/sobre/`, `/contacto/`, `/privacidade/`, `/termos/`, `/fontes-oficiais/`, `/sitemap.xml`, `/robots.txt`.
- Pesquisa global no header (`GlobalSearch`): input não-controlado (uncontrolled) para evitar bug de dead keys (ã, etc.) no macOS — usa `onInput` em vez de `onChange`/`value`.
- Ad slots desligados por defeito via `NEXT_PUBLIC_ENABLE_AD_SLOTS=true`; componente `AdSlot` retorna `null` quando flag está off.
- Breadcrumb em páginas de curso inclui link para faculdade: `Início > Cursos > Instituição > Curso`.
- Fontes oficiais movidas para coluna lateral (aside) com ícone de external link.
- Gráfico de evolução de notas (`GradesChart`) em Recharts: linhas por fase, tooltip custom, Y-axis ajustado ao range real. Só renderiza com ≥2 pontos de dados.
- `course_description` da sheet aparece em destaque (card) na página de curso quando existe.
- `institution_sigla` é pesquisável mas não aparece na UI.
- Coluna `course_description` adicionada à sheet e ao modelo de dados; mostrada na página de curso quando presente.
- Slugs de curso incluem nome do curso + instituição + código para evitar colisões.
- Política de privacidade e Termos em `/privacidade/` e `/termos/`, sem referências internas ao pipeline de dados.
- Página "O que encontras aqui" — removida a bullet point sobre arquitetura interna ("Páginas estáticas e indexáveis...").
- Link "Fontes oficiais" removido do topo; adicionado link "Faculdades".
- Logo e favicons colocados; favicon set inclui múltiplos tamanhos.
- Projeto DGES/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.
- Projeto DGAS/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.

## site-universidade — estado em 2026-05-10

- Projeto em `projects/site-universidade/`, Next.js App Router + TypeScript + Tailwind CSS, static export para Cloudflare Pages.
- Data source: Google Sheet `1m7LzrYoYTrCHYr3vBiaeK62ZOw-4GQVCbXse6d5FE6E`, importada via `gog` para `data/courses.csv`.
- Stack: Next.js, TypeScript, Tailwind CSS, Recharts (gráfico de notas), Papa Parse (CSV).
- Rotas geradas: `/`, `/cursos/`, `/cursos/[slug]/`, `/faculdades/`, `/faculdades/[slug]/`, `/sobre/`, `/contacto`, `/privacidade/`, `/termos/`, `/fontes-oficiais/`, `/sitemap.xml`, `/robots.txt`.
- Pesquisa global no header (`GlobalSearch`): input não-controlado (uncontrolled) para evitar bug de dead keys (ã, etc.) no macOS — usa `onInput` em vez de `onChange`/`value`.
- Ad slots desligados por defeito via `NEXT_PUBLIC_ENABLE_AD_SLOTS=true`; componente `AdSlot` retorna `null` quando flag está off.
- Breadcrumb em páginas de curso inclui link para faculdade: `Início > Cursos > Instituição > Curso`.
- Fontes oficiais movidas para coluna lateral (aside) com ícone de external link.
- Gráfico de evolução de notas (`GradesChart`) em Recharts: linhas por fase, tooltip custom, Y-axis ajustado ao range real. Só renderiza com ≥2 pontos de dados.
- `course_description` da sheet aparece em destaque (card) na página de curso quando existe.
- `institution_sigla` é pesquisável mas não aparece na UI.
- Coluna `course_description` adicionada à sheet e ao modelo de dados; mostrada na página de curso quando presente.
- Slugs de curso incluem nome do curso + instituição + código para evitar colisões.
- Política de privacidade e Termos em `/privacidade/` e `/termos/`, sem referências internas ao pipeline de dados.
- Página "O que encontras aqui" — removida a bullet point sobre arquitetura interna ("Páginas estáticas e indexáveis...").
- Link "Fontes oficiais" removido do topo; adicionado link "Faculdades".
- Logo e favicons colocados; favicon set inclui múltiplos tamanhos.

## site-universidade — mapa e distritos (continuação 2026-05-10)

- Pedido ativo do Tiago: continuar a afinar o mapa da homepage. Último feedback: “vamos tentar mais uma vez” com screenshot anotado.
- Requisitos do mapa na hero:
  - remover o espaço branco/cartão no topo para o conteúdo subir;
  - mapa sem card branco à volta;
  - continente à direita da hero;
  - Açores e Madeira à esquerda do continente, dentro das áreas aproximadas indicadas no screenshot, sem precisarem de contorno visível;
  - manter cores azul-claro/brand;
  - labels dos distritos devem ficar removidas;
  - informação de distrito só deve aparecer em hover;
  - ilhas foram pedidas ampliadas 4x no turno anterior, mas no screenshot atual devem ficar proporcionais e dentro das caixas indicadas.
- Estado do código antes da compactação:
  - componente principal: `projects/site-universidade/components/PortugalDistrictMap.tsx`;
  - caminhos do mapa: `projects/site-universidade/lib/portugalDistrictPaths.ts`;
  - homepage: `projects/site-universidade/app/page.tsx`.
- Já existem páginas agregadoras por distrito: `/distritos/` e `/distritos/[slug]/`, com `cidade`, `distrito`, `morada` no modelo `Course`.
- A sheet importada mostrou headers `cidade` e `distrito`; `morada` não apareceu no último output de headers.
- Próxima ação recomendada: inspecionar `PortugalDistrictMap.tsx` e `portugalDistrictPaths.ts`, ajustar `viewBox`/posições/escala das ilhas e remover/evitar labels se ainda existirem; depois correr `npm run typecheck`, `npm run lint`, `npm run build` em `projects/site-universidade` antes de responder.
- Projeto DGES/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.
- Projeto DGAS/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.

## site-universidade — estado em 2026-05-10

- Projeto em `projects/site-universidade/`, Next.js App Router + TypeScript + Tailwind CSS, static export para Cloudflare Pages.
- Data source: Google Sheet `1m7LzrYoYTrCHYr3vBiaeK62ZOw-4GQVCbXse6d5FE6E`, importada via `gog` para `data/courses.csv`.
- Stack: Next.js, TypeScript, Tailwind CSS, Recharts (gráfico de notas), Papa Parse (CSV).
- Rotas geradas: `/`, `/cursos/`, `/cursos/[slug]/`, `/faculdades/`, `/faculdades/[slug]/`, `/sobre/`, `/contacto/`, `/privacidade/`, `/termos/`, `/fontes-oficiais/`, `/sitemap.xml`, `/robots.txt`.
- Pesquisa global no header (`GlobalSearch`): input não-controlado (uncontrolled) para evitar bug de dead keys (ã, etc.) no macOS — usa `onInput` em vez de `onChange`/`value`.
- Ad slots desligados por defeito via `NEXT_PUBLIC_ENABLE_AD_SLOTS=true`; componente `AdSlot` retorna `null` quando flag está off.
- Breadcrumb em páginas de curso inclui link para faculdade: `Início > Cursos > Instituição > Curso`.
- Fontes oficiais movidas para coluna lateral (aside) com ícone de external link.
- Gráfico de evolução de notas (`GradesChart`) em Recharts: linhas por fase, tooltip custom, Y-axis ajustado ao range real. Só renderiza com ≥2 pontos de dados.
- `course_description` da sheet aparece em destaque (card) na página de curso quando existe.
- `institution_sigla` é pesquisável mas não aparece na UI.
- Coluna `course_description` adicionada à sheet e ao modelo de dados; mostrada na página de curso quando presente.
- Slugs de curso incluem nome do curso + instituição + código para evitar colisões.
- Política de privacidade e Termos em `/privacidade/` e `/termos/`, sem referências internas ao pipeline de dados.
- Página "O que encontras aqui" — removida a bullet point sobre arquitetura interna ("Páginas estáticas e indexáveis...").
- Link "Fontes oficiais" removido do topo; adicionado link "Faculdades".
- Logo e favicons colocados; favicon set inclui múltiplos tamanhos.
- Projeto DGES/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.
- Projeto DGAS/InfoCursos: o enriquecimento da sheet passou a usar colunas JSON no fim da folha, sem remover dados existentes; para as estatísticas do InfoCursos, guardar só dados relevantes do `Curso` quando aplicável, não incluir `source_url` dentro do JSON, e atualizar sempre `updated_at` com data+hora em Europe/Lisbon quando uma linha é alterada.
- O campo `course_description` foi acrescentado como nova coluna no fim da sheet e deve ser preenchido com descrições/pitches breves baseados em fontes oficiais, sem inventar.
- Para processamento em massa dos restantes cursos da sheet, a execução foi desenhada para ser conservadora com crawling/rate limiting, com pausas aleatórias e descansos por lote para reduzir risco de bloqueio.
- Refactor da sheet: as 6 colunas antigas de notas de entrada (`nota_ult_col_2023_1a`, `nota_ult_col_2023_2a`, `nota_ult_col_2024_1a`, `nota_ult_col_2024_2a`, `nota_ult_col_2025_1a`, `nota_ult_col_2025_2a`) foram consolidadas numa nova coluna JSON no fim, mantendo as colunas antigas intactas para não perder informação.

## site-universidade — estado em 2026-05-10

- Projeto em `projects/site-universidade/`, Next.js App Router + TypeScript + Tailwind CSS, static export para Cloudflare Pages.
- Data source: Google Sheet `1m7LzrYoYTrCHYr3vBiaeK62ZOw-4GQVCbXse6d5FE6E`, importada via `gog` para `data/courses.csv`.
- Stack: Next.js, TypeScript, Tailwind CSS, Recharts (gráfico de notas), Papa Parse (CSV).
- Rotas geradas: `/`, `/cursos/`, `/cursos/[slug]/`, `/faculdades/`, `/faculdades/[slug]/`, `/sobre/`, `/contacto`, `/privacidade/`, `/termos/`, `/fontes-oficiais/`, `/sitemap.xml`, `/robots.txt`.
- Pesquisa global no header (`GlobalSearch`): input não-controlado (uncontrolled) para evitar bug de dead keys (ã, etc.) no macOS — usa `onInput` em vez de `onChange`/`value`.
- Ad slots desligados por defeito via `NEXT_PUBLIC_ENABLE_AD_SLOTS=true`; componente `AdSlot` retorna `null` quando flag está off.
- Breadcrumb em páginas de curso inclui link para faculdade: `Início > Cursos > Instituição > Curso`.
- Fontes oficiais movidas para coluna lateral (aside) com ícone de external link.
- Gráfico de evolução de notas (`GradesChart`) em Recharts: linhas por fase, tooltip custom, Y-axis ajustado ao range real. Só renderiza com ≥2 pontos de dados.
- `course_description` da sheet aparece em destaque (card) na página de curso quando existe.
- `institution_sigla` é pesquisável mas não aparece na UI.
- Coluna `course_description` adicionada à sheet e ao modelo de dados; mostrada na página de curso quando presente.
- Slugs de curso incluem nome do curso + instituição + código para evitar colisões.
- Política de privacidade e Termos em `/privacidade/` e `/termos/`, sem referências internas ao pipeline de dados.
- Página "O que encontras aqui" — removida a bullet point sobre arquitetura interna ("Páginas estáticas e indexáveis...").
- Link "Fontes oficiais" removido do topo; adicionado link "Faculdades".
- Logo e favicons colocados; favicon set inclui múltiplos tamanhos.

## site-universidade — mapa e distritos (continuação 2026-05-10)

- Pedido ativo do Tiago: continuar a afinar o mapa da homepage. Último feedback: “vamos tentar mais uma vez” com screenshot anotado.
- Requisitos do mapa na hero:
  - remover o espaço branco/cartão no topo para o conteúdo subir;
  - mapa sem card branco à volta;
  - continente à direita da hero;
  - Açores e Madeira à esquerda do continente, dentro das áreas aproximadas indicadas no screenshot, sem precisarem de contorno visível;
  - manter cores azul-claro/brand;
  - labels dos distritos devem ficar removidas;
  - informação de distrito só deve aparecer em hover;
  - ilhas foram pedidas ampliadas 4x no turno anterior, mas no screenshot atual devem ficar proporcionais e dentro das caixas indicadas.
- Estado do código antes da compactação:
  - componente principal: `projects/site-universidade/components/PortugalDistrictMap.tsx`;
  - caminhos do mapa: `projects/site-universidade/lib/portugalDistrictPaths.ts`;
  - homepage: `projects/site-universidade/app/page.tsx`.
- Já existem páginas agregadoras por distrito: `/distritos/` e `/distritos/[slug]/`, com `cidade`, `distrito`, `morada` no modelo `Course`.
- A sheet importada mostrou headers `cidade` e `distrito`; `morada` não apareceu no último output de headers.
- Próxima ação recomendada: inspecionar `PortugalDistrictMap.tsx` e `portugalDistrictPaths.ts`, ajustar `viewBox`/posições/escala das ilhas e remover/evitar labels se ainda existirem; depois correr `npm run typecheck`, `npm run lint`, `npm run build` em `projects/site-universidade` antes de responder.

## site-universidade — flush pré-compactação 2026-05-10 22:43 Europe/Lisbon

- Último feedback do Tiago no Discord sobre a homepage map: está frustrado; disse “olha pra esta porcaria. nao conseguiste fazer o que te pedi. açores e madeira não estao onde eu disse.” Deve-se responder visivelmente no Discord com pedido de desculpa curto e assumir a correção direta antes de continuar.
- Estado atual do mapa: `components/PortugalDistrictMap.tsx` usa SVG `viewBox="0 0 520 520"`, sem labels/dots, sem card branco à volta, tooltip só quando existe `hovered`, distritos clicáveis para `/distritos/[slug]/`.
- Estado atual dos paths: `lib/portugalDistrictPaths.ts` foi gerado com mainland em área aproximada `(330,28,170,450)`, Açores em `(210,255,82,54)`, Madeira em `(210,345,82,48)`, ilhas com escala 4x. Provável problema: ilhas ainda estão demasiado à direita/perto do continente; devem ir muito mais à esquerda, nas posições do screenshot do utilizador.
- Recomendação técnica para próxima tentativa: deixar de depender apenas de paths pré-gerados se a posição continuar a falhar; separar mainland/Açores/Madeira em grupos/transforms SVG ou regenerar com áreas bem mais explícitas (Açores perto do lado esquerdo, Madeira abaixo à esquerda, continente à direita), mantendo escala uniforme para o continente.
- OG image já foi adicionada: `public/og/universidade-og.png`, dimensões 1536x1024, metadata em `app/layout.tsx` com OpenGraph e Twitter `summary_large_image`.
- Últimas validações passaram após retries limpos: `npm run typecheck`, `npm run lint`, `npm run build`. Build Next.js é intermitente/flaky neste ambiente; se falhar com erros de `.next`/export, fazer `rm -rf .next out` e repetir.
- Último commit conhecido após OG image: `5100767`.
