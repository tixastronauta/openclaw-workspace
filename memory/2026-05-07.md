# 2026-05-07

- Tiago is actively building a Google Drive/Sheets-based bank expenses aggregation workflow in `projects/bank-expenses-aggregator/`.
- Source Google Drive folder: `1ngT8XwYJ6L9FpYpO4XZ0Bg8nX9Ur4xq1`.
- Target Google Sheet ID used by the processor: `1OqVVZPawOU_8bakipfUVSrofmiJeJTiAbkZLtaDE20Y`; final Sheet title renamed to `Movimentos Bancários`.
- Project was renamed from `card-statements-aggregation` to `bank-expenses-aggregator` because it now handles both credit-card statements and current-account movements.
- Source files were renamed to format `YYYYMM-ExtratoCartao-Bank` and `YYYYMM-Movimentos-Bank` (extensions retained), e.g. `202602-ExtratoCartao-ActivoBank` and `202602-Movimentos-ActivoBank`.
- Main production script: `projects/bank-expenses-aggregator/process_expenses.py`.
- Processor responsibilities: discover files in Drive folder, parse supported PDFs/CSVs, keep expenses/debits only with positive `Valor`, apply `Regras Exclusao`, apply `Regras Catalogacao`, and overwrite `Movimentos` tab.
- Supported source formats after refactor: credit-card PDFs for ActivoBank, CGD, Unibanco, Universo; current-account CSVs for ActivoBank and CGD.
- Sheet tabs in use: `Movimentos`, `Categorias`, `Regras Catalogacao`, and `Regras Exclusao`.
- `Movimentos` columns established: `Banco`, `Tipo`, `Mes`, `Data`, `Descritivo`, `Valor`, `Categoria`, `Fonte`.
- `Data` rules: use ActivoBank card statement `Data Movimento`; use CGD card statement `Dt. Trans.`. Dates must be real Sheets dates, formatted `yyyy-mm-dd`, not text with leading apostrophe.
- `Valor` rules: include only expenses/debits; credits are not included; value must always be positive.
- `Tipo` distinguishes `Cartão` vs `Conta à ordem`.
- `Categorias` list includes: Viaturas - Via Verde, Viaturas - Manutenção, Viaturas - Combustível, Viagens, Vestuário, Subscrições, Seguros, Saúde, Restauração, Presentes, Outros, Impostos e Taxas, Hobbies, Ginásio e bem estar, Entretenimento, Compras Online, Casa - Outros, Casa - Limpeza, Casa - Eletrodomésticos/Mobília, Casa - Contas, ATM, Animais, Alimentação.
- Categorisation workflow: user can add rules in `Regras Catalogacao`; processor must consult them whenever categorising. If confidence is low and no rule matches, leave `Categoria` blank.
- Known categorisation rules/examples: `PLEASANTDIALOG` means veterinarian payments and should be `Animais`; PayPal with `Valor` 17.99 is YouTube Premium and should be `Subscrições`; `BX VALOR` was changed by Tiago to `Viaturas - Via Verde`.
- `Regras Catalogacao` supports at least active flag, priority, matching text, optional value, match type, optional bank/type filters, category, notes; headers have notes explaining usage.
- Tiago asked for ability to reapply rules to existing Sheet rows without rereading source files; this was confirmed possible and used repeatedly.
- Exclusion workflow: `Regras Exclusao` tab was created and processor uses it to remove non-expense account movements.
- Exclusion rules currently used to avoid double-counting credit-card payments from current-account movements. Tiago approved the identified candidates.
- Tiago also intends to exclude transfers to himself/other accounts, e.g. descriptions containing `TFI tiago carvalho`, by adding a `Regras Exclusao` rule; no action was taken at that point except explaining the approach.
- Important behavior: When Tiago says “reprocessa regras de exclusao e regras de catalogacao” or “again”, reapply exclusion and categorisation rules to the Sheet; recent runs reported around 14 excluded and 492 final rows, though exact counts can change as rules change.
- A monthly expenses chart was briefly added to the first sheet then removed at Tiago’s request. Do not recreate it unless asked.
- Tiago was frustrated earlier because initial work mistakenly used CSV account movements when he had requested credit-card PDFs only; be careful to distinguish source types and explicitly verify Drive files when asked.
- Added `Email Import Config` tab to `Movimentos Bancários` with monthly Gmail filters for credit-card statements: Unibanco, ActivoBank, Universo, CGD.
- Added `projects/bank-expenses-aggregator/import_card_statement_emails.py`: reads Sheet config, searches Gmail, downloads matching PDF attachments, uploads them to Drive source folder using `{YYYYMM}-ExtratoCartao-Banco.pdf`, and summarizes results/manual blockers.
- Dry-run for 202604 succeeded logically: Unibanco PDF found; ActivoBank and Universo already existed in Drive; CGD email found but only `headercdo.jpg` and `smime.p7s`, no PDF, so it should be reported as manual site download.
- Scheduled monthly cron job `Import monthly credit-card statement PDFs from Gmail` for day 3 at 09:00 Europe/Lisbon, delivered to Telegram `6384494297`, because ActivoBank statements can arrive after month-end (e.g. 2026-05-02 for 202604).
